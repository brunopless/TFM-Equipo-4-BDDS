---
title: "Stepwise"
author: "AC"
date: "2024-09-12"
output:
  html_document: default
  pdf_document: default
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

# Instalamos Matric para el posterior cálculo del MAE
```{r}
#install.packages("Metrics")
#install.packages("MASS")  # Si no lo tienes instalado
library(Metrics)
library(MASS)
```



```{r}
url <- "https://raw.githubusercontent.com/brunopless/TFM-Equipo-4-BDDS/main/entrega-3/df_bcn_imputado.csv"
download.file(url, destfile = "df_bcn_imputado.csv", mode = "wb")

library(readr)
data <- read_csv("df_bcn_imputado.csv")



```
#Limpieza del dataset

##Limpiar media_precios_airbnb_500

Tenemos 4 valores perdidos.

```{r}
data <- data[!is.na(data$media_precios_airbnbs_500), ]
```

##Limpiar newDevelopment

Tenemos 1 valor perdido.

```{r}
data <- data[!is.na(data$newDevelopment), ]
```

##Limpiar floors y bathrooms

Tenemos 11 valores perdidos. Basta con limpiar solo una variable.

```{r}
data <- data[!is.na(data$rooms), ]
data <- data[!is.na(data$floor), ]
```

### para casos en que exterior es nulo, le asignaremos el valor 0.

```{r}
data$exterior[is.na(data$exterior)] <- 0
```

##Crear log_price

```{r}
# Crear una nueva columna con los logaritmos naturales
data$log_price_m2 <- log(data$price_m2)
```

```{r}
head(data)
```

##Seleccionar variables
```{r}
numeric_vars <- names(data)[sapply(data, is.numeric)]
```

### Remover variables no relevantes para al modelo de regresión lineal
```{r}
# Variables a eliminar
remove_vars <- c("Unnamed: 0", "index","latitude", "longitude", "price", "Fecha extraccion", "isParkingSpaceIncludedInPrice", "ParkingSpacePrice", "log_price","log_price_m2")

print(numeric_vars)
```
## Variables a considerar para el modelo
```{r}
# Filtrando las variables
variables_SAR <- setdiff(numeric_vars, remove_vars)
print(variables_SAR)
```



##Entrenar modelo de regresión lineal

```{r}
formula <- as.formula(paste("log_price ~", paste(variables_SAR, collapse=" + ")))

formula
```
## Formula con las varibles a considerar dentro del modelo de regresión lineal
```{r}
# Ajustar el modelo completo
modelo_completo <- lm(log_price_m2 ~ floor + size + exterior + rooms + bathrooms + 
    newDevelopment + hasLift + topNewDevelopment + numero_bancos_cercanos + 
    numero_starbucks_cercanos + numero_atractivos_cercanos + 
    numero_buenasmigas_cercanos + numero_clubesnocturnos_cercanos + 
    numero_mcdonalds_cercanos + numero_metro_cercanos + numero_parking_cercanos + 
    numero_parkingbici_cercanos + numero_parques_cercanos + numero_playas_cercanos + 
    numero_santagloria_cercanos + numero_vivari_cercanos + num_airbnbs_500 + 
    media_precios_airbnbs_500 + propertyType_chalet + 
    propertyType_duplex + propertyType_flat + propertyType_penthouse + 
    status_good + status_newdevelopment + num_airbnbs_500_mas_de_cero + 
    media_precios_airbnbs_500_mas_de_cero + numero_bancos_cercanos_mas_de_cero + 
    numero_starbucks_cercanos_mas_de_cero + numero_atractivos_cercanos_mas_de_cero + 
    numero_buenasmigas_cercanos_mas_de_cero + numero_clubesnocturnos_cercanos_mas_de_cero + 
    numero_mcdonalds_cercanos_mas_de_cero + numero_metro_cercanos_mas_de_cero + 
    numero_parking_cercanos_mas_de_cero + numero_parkingbici_cercanos_mas_de_cero + 
    numero_parques_cercanos_mas_de_cero + numero_playas_cercanos_mas_de_cero + 
    numero_santagloria_cercanos_mas_de_cero + numero_vivari_cercanos_mas_de_cero, 
                    data = data)

modelo_nulo <- lm(log_price_m2 ~ 1, data = data)
```

## Def. stepwise forward
```{r}
# Aplicar stepwise
modelo_stepwise <- stepAIC(modelo_nulo, 
                                   scope = list(lower = modelo_nulo, upper = modelo_completo), 
                                   direction = "forward")
```

```{r}
summary(modelo_stepwise)
```


### Cálculo del MAE
```{r}
# Obtención de predicciones
predicciones <- predict(modelo_stepwise, newdata = data)

# Obtener los valores reales
valores_reales <- data$log_price

# Calcular el MAE manualmente
errores_absolutos <- abs(predicciones - valores_reales)
mae_manual <- mean(errores_absolutos)

#Obtener AIC:
AIC <- AIC(modelo_stepwise)

print(paste("El MAE manual es:", mae_manual))
print(paste("El AIC es:", AIC))
```

### Cálculo del MAPE
# Calcular el MAPE manualmente
```{r}
errores_porcentuales <- abs((predicciones - valores_reales) / valores_reales) * 100
mape <- mean(errores_porcentuales, na.rm = TRUE)  # na.rm = TRUE para manejar posibles NA

print(paste("El MAPE es:", mape))
```



## Obtenemos los valores de los residuos y los valores de los residuos ajustados para el modelo
```{r}
residuos <- residuals(modelo_stepwise)
valores_ajustados <- fitted(modelo_stepwise)
```

## Gráficamos los residuos en función de los valores ajustados y los índices de los datos
```{r}
# Residuos vs Valores Ajustados
plot(valores_ajustados, residuos, 
     xlab = "Valores Ajustados", 
     ylab = "Residuos", 
     main = "Residuos vs Valores Ajustados")
abline(h = 0, col = "red", lwd = 2)

# Residuos vs Índices
plot(residuos, 
     xlab = "Índice", 
     ylab = "Residuos", 
     main = "Residuos vs Índices")
abline(h = 0, col = "red", lwd = 2)
```
### Gráficamos el histograma de los residuos para visualizar la distribución de estos
```{r}
hist(residuos, 
     main = "Histograma de Residuos", 
     xlab = "Residuos", 
     col = "lightblue", 
     breaks = 20)
```
### Gráficamos el Q-Q Plot para veríficar si los residuos siguen una distribución normal

```{r}
qqnorm(residuos)
qqline(residuos, col = "red")
```


## Def. stepwise backward
```{r}
# Aplicar stepwise
modelo_stepwise <- stepAIC(modelo_completo, direction = "backward")
```


```{r}
summary(modelo_stepwise)
```
### Cálculo del MAE
```{r}
# Obtención de predicciones
predicciones <- predict(modelo_stepwise, newdata = data)

# Obtener los valores reales
valores_reales <- data$log_price

# Calcular el MAE manualmente
errores_absolutos <- abs(predicciones - valores_reales)
mae_manual <- mean(errores_absolutos)

#Obtener AIC:
AIC <- AIC(modelo_stepwise)

print(paste("El MAE manual es:", mae_manual))
print(paste("El AIC es:", AIC))
```
### Cálculo del MAPE
# Calcular el MAPE manualmente
```{r}
errores_porcentuales <- abs((predicciones - valores_reales) / valores_reales) * 100
mape <- mean(errores_porcentuales, na.rm = TRUE)  # na.rm = TRUE para manejar posibles NA

print(paste("El MAPE es:", mape))
```

## Obtenemos los valores de los residuos y los valores de los residuos ajustados para el modelo
```{r}
residuos <- residuals(modelo_stepwise)
valores_ajustados <- fitted(modelo_stepwise)
```

## Gráficamos los residuos en función de los valores ajustados y los índices de los datos
```{r}
# Residuos vs Valores Ajustados
plot(valores_ajustados, residuos, 
     xlab = "Valores Ajustados", 
     ylab = "Residuos", 
     main = "Residuos vs Valores Ajustados")
abline(h = 0, col = "red", lwd = 2)

# Residuos vs Índices
plot(residuos, 
     xlab = "Índice", 
     ylab = "Residuos", 
     main = "Residuos vs Índices")
abline(h = 0, col = "red", lwd = 2)
```
### Gráficamos el histograma de los residuos para visualizar la distribución de estos
```{r}
hist(residuos, 
     main = "Histograma de Residuos", 
     xlab = "Residuos", 
     col = "lightblue", 
     breaks = 20)
```
### Gráficamos el Q-Q Plot para veríficar si los residuos siguen una distribución normal

```{r}
qqnorm(residuos)
qqline(residuos, col = "red")
```


## Def. stepwise both
```{r}
# Aplicar stepwise
modelo_stepwise <- stepAIC(modelo_completo, direction = "both")
```

```{r}
summary(modelo_stepwise)
```
### Cálculo del MAE
```{r}
# Obtención de predicciones
predicciones <- predict(modelo_stepwise, newdata = data)

# Obtener los valores reales
valores_reales <- data$log_price

# Calcular el MAE manualmente
errores_absolutos <- abs(predicciones - valores_reales)
mae_manual <- mean(errores_absolutos)

#Obtener AIC:
AIC <- AIC(modelo_stepwise)

print(paste("El MAE manual es:", mae_manual))
print(paste("El AIC es:", AIC))
```
### Cálculo del MAPE
# Calcular el MAPE manualmente
```{r}
errores_porcentuales <- abs((predicciones - valores_reales) / valores_reales) * 100
mape <- mean(errores_porcentuales, na.rm = TRUE)  # na.rm = TRUE para manejar posibles NA

print(paste("El MAPE es:", mape))
```


## Obtenemos los valores de los residuos y los valores de los residuos ajustados para el modelo
```{r}
residuos <- residuals(modelo_stepwise)
valores_ajustados <- fitted(modelo_stepwise)
```

## Gráficamos los residuos en función de los valores ajustados y los índices de los datos
```{r}
# Residuos vs Valores Ajustados
plot(valores_ajustados, residuos, 
     xlab = "Valores Ajustados", 
     ylab = "Residuos", 
     main = "Residuos vs Valores Ajustados")
abline(h = 0, col = "red", lwd = 2)

# Residuos vs Índices
plot(residuos, 
     xlab = "Índice", 
     ylab = "Residuos", 
     main = "Residuos vs Índices")
abline(h = 0, col = "red", lwd = 2)
```
### Gráficamos el histograma de los residuos para visualizar la distribución de estos
```{r}
hist(residuos, 
     main = "Histograma de Residuos", 
     xlab = "Residuos", 
     col = "lightblue", 
     breaks = 20)
```
### Gráficamos el Q-Q Plot para veríficar si los residuos siguen una distribución normal

```{r}
qqnorm(residuos)
qqline(residuos, col = "red")
```